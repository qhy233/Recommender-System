# 🚀 创新杯备赛计划书：基于大模型智能体的心理学调查仿真系统 (Ver 2.0)

> **项目代号**：Project Psy-Agent
> **参赛赛道**：选题 4 - 基于大模型智能体的心理学调查仿真系统
> **提交截止**：2026年3月2日
> **核心理念**：利用“硅基采样（Silicon Sampling）”技术，构建低成本、高效率的群体心理仿真实验室。

---

## 一、 项目核心概念 (Concept)

### 1.1 我们做什么？
我们不开发心理咨询聊天机器人，我们开发的是一个“数字化心理实验室”。
系统能够批量生成具有特定人格设定（如：抑郁倾向、外向、高压人群）的 AI 智能体（Agents），让它们像真人一样完成标准的心理学量表，并自动生成信度、效度分析报告。

### 1.2 核心价值（为什么做这个？）
* **省时省钱**：传统心理学找 1000 个被试需要数万元经费和数月时间，我们只需要几分钟和几块钱 API 成本。
* **社会预演**：可以模拟极端场景（如“遭遇重大挫折”）下的群体心理反应，而无需对真人进行伦理上有风险的实验。

---

## 二、 实验设计与科学性验证 (Experiment Design)

**这是本项目区别于普通软件开发的关键，也是利用您《心理测量学》背景的得分点。**

### 🧪 实验 I：结构效度验证 (Construct Validity)
* **目的**：验证 AI 是否真的“听懂”了人设。
* **操作**：
    * **A组**：设定 50 个“极度内向”的 Agent。
    * **B组**：设定 50 个“极度外向”的 Agent。
    * **测试**：让它们填写《大五人格量表 (BFI)》。
* **预期结果**：B组在“外向性”维度得分显著高于A组 (T-test, P < 0.05)。

### 🧪 实验 II：复测信度验证 (Test-Retest Reliability)
* **目的**：验证系统的稳定性，证明 AI 不是在随机乱选。
* **操作**：
    * 对同一批 Agent，在 T1 时刻测一次，T2 时刻（清除记忆后）再测一次。
* **预期结果**：计算两次得分的 **皮尔逊相关系数 (Pearson Correlation)**，目标值 > 0.7。

### 🧪 实验 III：焦虑情境模拟 (Scenario Simulation)
* **目的**：展示应用价值。
* **操作**：
    * 设定 Agent 为“面临期末考试且挂科风险极大的大学生”。
    * 测试其在 **SAS (焦虑自评量表)** 上的得分。
* **预期结果**：得分应显著高于常模（普通人群平均分）。

---

## 三、 系统架构与技术实现 (Tech Stack)

### 3.1 技术栈
* **编程语言**：Python 3.10+
* **核心模型**：DeepSeek-V3 (推荐) 或 OpenAI GPT-4o-mini (通过 API 调用)
* **数据处理**：Pandas, NumPy
* **统计分析**：`pingouin` (专门做心理统计的 Python 库), `scipy`
* **可视化/UI**：Streamlit (极速构建 Web 界面)

### 3.2 核心机制：Agent 是如何构建的？ (Crucial Logic)
**Agent 不是独立的软件程序，而是“参数化的 Prompt 配置”。**
* **PaaS 模式**：我们不需要下载 1000 个模型，也不需要 1000 个账号。
* **实现原理**：
    1.  **静态数据层**：用 JSON/Excel 定义 1000 个“人设字典”（包含年龄、性格、经历）。
    2.  **动态仿真层**：编写一个 Python 循环，每次读取一个人设，将其注入到大模型的 `System Prompt` 中，调用同一个 API 接口。
    3.  **数据流向**：Excel (人设库) -> Python (组装Prompt) -> DeepSeek API -> Python (解析返回数字) -> CSV (结果库)。

### 3.3 系统数据流图
graph LR
    A[用户输入: 设定人设 & 选择量表] --> B(角色生成模块: 生成 Prompts);
    B --> C{仿真引擎: 循环调用 API};
    D[量表题目库] --> C;
    C --> E[数据清洗: 解析 JSON/数字结果];
    E --> F[分析模块: 计算 Cronbach's Alpha, T检验];
    F --> G[前端展示: 自动生成图表报告];