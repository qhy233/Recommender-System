# 基础知识笔记-推荐系统基础

## 零、推荐系统概述

推荐系统的本质是对关系的量化预测，其核心不仅在于技术指标，更在于构建健康的生态系统。理解推荐系统需从三个维度展开：

### 1. 微观视角：关系预测
推荐系统的原子任务是预测用户与物品建立连接的可能性。
* **核心函数**：$Score = f(User, Item, Context)$。
* **三大要素**：
    * **用户 (User)**：历史行为、显式反馈、实时意图等。
    * **物品 (Item)**：内容属性、统计属性（热度、评分）。
    * **场景 (Context)**：时间、地点、当前状态等。

### 2. 工业视角：漏斗架构
为了解决“海量候选 vs 有限算力/时间”的矛盾，工业界采用分阶段的漏斗式架构：
* **召回 (Recall)**：
    * **目标**：快速筛选，追求覆盖度（海量 $\rightarrow$ 千级）。
    * **策略**：模型相对简单，速度快，“宁可错杀一千，不可放过一个”。
* **排序 (Ranking)**：
    * **目标**：精准预测，追求准确率（千级 $\rightarrow$ 百级）。
    * **策略**：利用复杂深度模型，特征最全，计算成本高。
* **重排 (Re-ranking)**：
    * **目标**：优化体验，追求全局最优（百级 $\rightarrow$ 最终列表）。
    * **策略**：考虑多样性、新颖性、公平性及业务规则。

### 3. 宏观视角：生态平衡
优秀的推荐系统需要避免“准确率陷阱”（即只推荐用户已知的高转化物品），致力于构建多方共赢的生态。
* **生态三角**：平衡 **用户/创作者**（需求/供给）、**内容**（媒介）、**平台**（调控）三者利益。
* **目标**：在短期指标（点击率）与长期健康（多样性、创作者激励）之间寻找动态平衡。

## 一、召回模型
### 协同过滤

协同过滤（Collaborative Filtering, CF）的核心思想是利用群体智慧，基于用户历史行为数据进行推荐。

主要分为两大类：
1.  **基于邻域的方法**（Neighborhood-based）：直接计算用户或物品间的相似度。
2.  **基于模型的方法**（Model-based）：通过学习隐向量（Latent
    Vector）来预测偏好，典型代表是矩阵分解。

#### 1. 基于邻域的协同过滤

##### 1.1 基于物品的协同过滤 (ItemCF)

* **核心思想**：假设用户的兴趣具有连贯性，推荐与用户“过去喜欢的物品”相似的物品。
* **工业界优势**：ItemCF在工业界应用更广，因为物品的属性相对稳定，物品相似度矩阵的更新成本低于用户相似度矩阵。

**关键公式：**

1.  **物品相似度 (余弦相似度 - 无评分)**：
    $$w_{ij} = \frac{C[i][j]}{\sqrt{|N(i)| \cdot |N(j)|}}$$
    * $C[i][j]$：同时喜欢物品 $i$ 和 $j$ 的用户数。
    * $|N(i)|$：喜欢物品 $i$ 的总用户数。

2.  **物品相似度 (皮尔逊相关系数 - 有评分)**：
    $$w_{ij} = \frac{\sum_{u \in U}(r_{ui} - \bar{r}_i)(r_{uj} - \bar{r}_j)}{\sqrt{\sum_{u \in U}(r_{ui} - \bar{r}_i)^2}\sqrt{\sum_{u \in U}(r_{uj} - \bar{r}_j)^2}}$$
    * $r_{ui}$：用户 $u$ 对物品 $i$ 的评分。
    * $\bar{r}_i$：物品 $i$ 获得的平均评分。

3.  **预测评分 (有评分)**：
    $$\hat{r}_{u,j} = \bar{r}_{j} + \frac{\sum_{k \in S_j} w_{jk}\,( r_{u,k} - \bar{r}_{k} )}{\sum_{k \in S_j} w_{jk}}$$
    * $S_j$：与物品 $j$ 最相似的物品集合。
    * $w_{jk}$：物品 $j$ 和 $k$ 的相似度。

**ItemCF 改进：Swing 算法**

* **核心思想**：通过分析用户-物品二部图的结构来过滤噪音。其洞察是：如果多个用户在其他共同购买行为较少的情况下，同时购买了同一对物品，那么这对物品之间的关联性就更可信。
* **基础公式 (Swing Score)**：
    $$s(i, j) = \sum_{u \in U_i \cap U_j} \sum_{v \in U_i \cap U_j} \frac{1}{\alpha + |I_u \cap I_v|}$$
    * $U_i, U_j$：分别与物品 $i, j$ 交互的用户集合。
    * $I_u, I_v$：分别被用户 $u, v$ 交互的物品集合。
    * $\alpha$：平滑系数。

**ItemCF 改进：Surprise 算法（互补品推荐）**

* **核心思想**：Surprise 算法是 Swing 论文中提出的，**专门用于解决互补商品推荐**的算法。它基于 Swing Score，但进一步考虑了互补关系的**时序性**和**方向性**（例如，先买手机、再买手机壳）。
* 该算法通过三个层面来衡量互补相关性，并使用商品聚类来缓解数据稀疏问题。

**关键公式：**

1.  **类别层面 (Category Level)**：
    * 用于筛选相关的类别对。
    * $\theta_{i,j} = p(c_{i,j} \mid c_j) = \frac{N(c_{i,j})}{N(c_j)}$
    * $N(c_{i,j})$：先买类别 $i$ 后买类别 $j$ 的次数。

2.  **商品层面 (Item Level)**：
    * 考虑购买顺序和时间间隔。
    * $s_{1}(i, j) = \frac{\sum_{u \in U_i \cap U_j} \frac{1}{1 + | t_{ui} - t_{uj} |}}{\| U_i \| \times \| U_j \|}$
    * *条件*：此公式仅在 $j$ 属于 $i$ 的相关类别且购买时间晚于 $i$ ($t_{uj} > t_{ui}$) 时才计入。

3.  **聚类层面 (Cluster Level)**：
    * 使用标签传播算法 (Label Propagation) 对商品进行聚类，以聚合稀疏信号。
    * $s_2(i,j) = s_1(L(i), L(j))$
    * $L(i)$ 是商品 $i$ 的聚类标签。

4.  **最终线性组合 (Final Combination)**：
    * $s(i, j) = \omega \cdot s_{1}(i, j) + (1 - \omega) \cdot s_{2}(i, j)$
    * $\omega$ 是权重超参数。

##### 1.2 基于用户的协同过滤 (UserCF)

* **核心思想**：推荐“相似用户”喜欢的物品。假设“具有相似历史行为的用户，未来偏好也相似”。

**关键公式：**

1.  **用户相似度 (Jaccard)**：
    $$w_{uv} = \frac{|N(u) \cap N(v)|}{|N(u) \cup N(v)|}$$
    * $N(u), N(v)$：用户 $u$ 和 $v$ 各自喜欢的物品集合。

2.  **用户相似度 (Cosine)**：
    $$w_{uv} = \frac{|N(u) \cap N(v)|}{\sqrt{|N(u)|\cdot|N(v)|}}$$

3.  **用户相似度 (Pearson)**：
    $$w_{uv} = \frac{\sum_{i \in I}(r_{ui} - \bar{r}_u)(r_{vi} - \bar{r}_v)}{\sqrt{\sum_{i \in I}(r_{ui} - \bar{r}_u)^2}\sqrt{\sum_{i \in I}(r_{vi} - \bar{r}_v)^2}}$$
    * $r_{ui}$：用户 $u$ 对物品 $i$ 的评分。
    * $\bar{r}_u$：用户 $u$ 的平均评分。

4.  **预测评分 (考虑偏置)**：
    $$\hat{r}_{u,p} = \bar{r}_{u} + \frac{\sum_{v \in S_u} w_{uv} \, (r_{v,p} - \bar{r}_{v})}{\sum_{v \in S_u} w_{uv}}$$
    * $S_u$：与用户 $u$ 最相似的邻居集合。
    * $\bar{r}_v$：邻居 $v$ 的平均评分。

**UserCF 优化策略：基于物品倒排表的优化**

* 很多用户对之间没有共同行为的物品，相似度必然为0。
* **优化步骤**：
    1.  **构建倒排表**：为每个物品维护一个用户列表（哪些用户对这个物品有过行为）。
    2.  **稀疏矩阵计算**：创建一个矩阵 $C[u][v]$ 记录用户 $u$ 和 $v$ 的共同物品数量。遍历每个物品的用户列表，将列表中的用户两两配对，对应的 $C[u][v]$ 值加1。
    3.  **计算最终相似度**：矩阵 $C$ 得到了余弦相似度的分子，再除以分母 $\sqrt{|N(u)||N(v)|}$ 即可。
    4.  **生成推荐**：使用 $p(u, i) = \sum_{v \in S_u \cap N(i)} w_{uv} r_{vi}$ 计算兴趣分。

#### 2. 基于模型的协同过滤：矩阵分解 (MF)

* **核心思想**：MF标志着CF从邻域方法向向量化表示的范式转变，旨在解决邻域方法的稀疏性问题。
* **基本假设**：低秩假设，隐向量假设。假设用户的偏好和物品的特征可以由少数几个“隐含因子”来描述。通过学习用户和物品的隐向量（Embeddings），用向量内积来预测评分。

**关键公式：**

##### 2.1 基础模型 (FunkSVD)

* **预测公式**：
    $$\hat{r}_{ui} = p_u^T q_i = \sum_{k=1}^{K} p_{u,k} \cdot q_{i,k}$$
    * $p_u$：用户 $u$ 的 $K$ 维隐向量。
    * $q_i$：物品 $i$ 的 $K$ 维隐向量。
    * $\hat{r}_{ui}$：预测用户对物品的评分,这两个向量的内积
    * $p_{u,k}$：用户u在第k个隐含因子上的偏好程度
    * $q_{i,k}$：物品i在第k个隐含因子上的特征强度
* **目标函数 (含L2正则化)**：
    $$\min_{P,Q} \frac{1}{2} \sum_{(u,i)\in \mathcal{K}} \left( r_{ui} - p_u^T q_i \right)^2 + \lambda \left( \|p_u\|^2 + \|q_i\|^2 \right)$$
    * $\mathcal{K}$：所有已知评分的用户-物品对集合。
    * $\lambda$：正则化系数。

##### 2.2 改进模型 (BiasSVD)

* **核心思想**：在基础模型上引入偏置项，以消除不同用户评分习惯和物品受欢迎程度带来的系统性偏差。

* **预测公式**：
    $$\hat{r}_{ui} = \mu + b_u + b_i + p_u^T q_i$$
    * $\mu$：全局平均评分。
    * $b_u$：用户 $u$ 的个人偏置。
    * $b_i$：物品 $i$ 的偏置。

* **目标函数 (含L2正则化)**：
    $$\min_{P,Q,b_u,b_i} \frac{1}{2} \sum_{(u,i)\in \mathcal{K}} \left( r_{ui} - \mu - b_u - b_i - p_u^T q_i \right)^2 + \lambda \left( \|p_u\|^2 + \|q_i\|^2 + b_u^2 + b_i^2 \right)$$

#### 3. 总结：局限性与贡献

* **核心局限性**：
    1.  **冷启动 (Cold Start)**：无法为缺乏历史数据的新用户或新物品提供推荐。
    2.  **数据稀疏性 (Sparsity)**：邻域方法在数据稀疏时难以找到可靠的“邻居”。
* **核心贡献**：
    * 矩阵分解开启的 **“向量化表示”** 思想，为后续的深度学习推荐模型奠定基础。

### 向量召回 

向量召回的核心思想是将用户和物品从离散的 ID 符号映射到连续的向量空间中，让向量间的“距离”代表语义上的“相似度”。

* **核心变革**：将原本复杂度为 $O(U \times I)$ 的“逐一匹配”问题，转化为复杂度为 $O(\log I)$ 的“向量搜索” (ANN) 问题。
* **技术路径**：主要分为 **i2i (Item-to-Item)** 和 **u2i (User-to-Item)** 两大类。

#### 1. i2i 召回 (Item-to-Item)

核心任务是计算物品与物品之间的相似性，本质是对**序列 (Sequence)** 的建模与利用。

##### 1.1 Item2Vec

* **核心思想**：基于 NLP 领域的 Word2Vec (Skip-Gram) 思想。将用户行为序列视为“句子”，物品视为“词”。假设在相似上下文中出现的物品具有相似的语义。
* **局限性**：忽略了时序信息（将历史视为集合而非序列），且无法处理冷启动物品。

**关键公式 (Skip-Gram 条件概率)**：
$$P(w_{t+j} | w_t) = \frac{e^{v_{w_{t+j}}^T v_{w_t}}}{\sum_{k=1}^{|V|} e^{v_{w_k}^T v_{w_t}}}$$
* $w_t$：中心词（当前物品）。
* $w_{t+j}$：上下文词（共现物品）。
* 实际训练通常采用 **负采样 (Negative Sampling)** 优化。

##### 1.2 EGES (Enhanced Graph Embedding with Side Information)

* **核心思想**：由阿里巴巴提出。通过引入**辅助信息 (Side Information)** 解决冷启动问题，并构建**商品关系图**来捕捉短期兴趣。
* **关键创新**：**加权聚合**。不同属性（如品牌、价格）对不同商品的重要性不同，模型自动学习这些权重。

**关键公式 (加权平均聚合)**：
$$H_v = \frac{\sum_{j=0}^n e^{a_v^j} W_v^j}{\sum_{j=0}^n e^{a_v^j}}$$
* $W_v^0$：商品 ID 的向量表示。
* $W_v^j$ ($j>0$)：第 $j$ 个辅助信息（如品牌）的向量表示。
* $a_v^j$：第 $j$ 个属性的可学习权重。

##### 1.3 Airbnb 召回模型

* **核心思想**：将 **业务目标（最终预订）** 融入序列学习。不仅仅学习“点击相似性”，更学习“转化相关性”。
* **核心策略**：
    1.  **全局上下文 (Global Context)**：无论序列多长，始终将用户最终**预订**的房源作为正样本放入训练，强化转化信号。
    2.  **同市场负采样 (Market-aware Negative Sampling)**：强制模型在同一地理市场内区分正负样本，迫使模型学习比地理位置更细粒度的特征。

**关键公式 (含全局上下文的目标函数)**：
$$\mathcal{L} \approx \sum \log \sigma(v_{l^+}^T v_l) + \mathbf{\log \sigma(v_{l_b}^T v_l)} + \text{NegSamples}$$
* $v_{l_b}$：用户最终预订房源的向量（Global Context）。这意味着序列中的每一次滑动窗口更新，都会受到最终预订结果的指引。

#### 2. u2i 召回 (User-to-Item)

核心是**双塔架构 (Two-Tower Model)**。将用户和物品分别编码为向量，通过点积或余弦相似度衡量匹配程度。

* **通用优势**：**离线/实时解耦**。物品向量 $V_{item}$ 离线计算建索引，用户向量 $V_{user}$ 实时计算，复杂度降至 $O(U+I)$。
* **通用公式**：
    $$\text{Score} = V_{user} \cdot V_{item}^T$$

##### 2.1 FM (因子分解机)

* **核心思想**：双塔模型的雏形。将二阶特征交互 $\langle v_i, v_j \rangle$ 分解，通过代数变换推导出双塔形式。
* **双塔分解**：
    * 用户向量 $V_{user}$：包含用户一阶特征和 Embedding 的 Sum Pooling。
    * 物品向量 $V_{item}$：包含物品一阶特征和 Embedding 的 Sum Pooling（以及复杂的自交互项）。

##### 2.2 DSSM (深度结构化语义模型)

* **核心思想**：标准的深度学习双塔。用 **DNN** 替代 FM 的线性变换，学习更复杂的非线性特征。
* **训练范式**：极端多分类问题 (Sampled Softmax)。

**关键细节 (工程 Trick)**：
1.  **L2 归一化**：$u \leftarrow u/||u||$。将向量点积转化为欧式距离度量，保证训练（点积最大化）与检索（距离最小化）的一致性。
2.  **温度系数 $\tau$**：$s(u,v) = \langle u,v \rangle / \tau$。调节 Softmax 分布的锐利程度，控制模型预测的“确定性”。

##### 2.3 YouTubeDNN

* **核心思想**：**Next Watch Prediction**。将召回定义为预测用户下一个观看的视频（Next Token Prediction）。
* **架构特点**：**非对称双塔**。
    * **用户塔**：极度复杂。融合观看历史、搜索历史、人口统计、**Example Age (视频新鲜度)** 等多模态特征。
    * **物品塔**：极度简单。本质上就是 Softmax 层的权重矩阵 (Embedding Matrix)。
* **关键策略**：
    * **时序分割 (Temporal Split)**：训练时严格屏蔽“未来信息”，只使用当前时刻之前的历史行为，防止标签泄露。
    * **负采样**：使用 Importance Sampling 进行高效训练。

**关键公式 (多分类概率)**：
$$P(w_t=i|U,C) = \frac{e^{v_i \cdot u}}{\sum_{j \in V} e^{v_j \cdot u}}$$
* $u$：用户塔输出的向量。
* $v_i$：物品 $i$ 对应的 Embedding 向量。

### 三、序列召回 (Sequential Retrieval)

序列召回标志着推荐理念的深刻转变：从为用户绘制静态的兴趣“快照”（如双塔模型），转向理解并续写用户行为的动态“故事”。其核心目标不再是回答“用户是谁”，而是预测 **“用户下一步会做什么”**。

主要分为两条技术路径：
1.  **深化用户表示**：用更复杂的结构（多向量、长短期结构）来立体地刻画用户。
2.  **生成式召回**：将推荐问题重塑为序列生成问题（Next Item Prediction）。

#### 1. 深化用户兴趣表示

传统方法将用户历史“压扁”成单一向量，难以表达用户的**多样性兴趣**（如同时关注编程和育儿）以及**兴趣的时效性**（长期偏好 vs 即时需求）。

##### 1.1 MIND (Multi-Interest Network with Dynamic Routing)

* **核心思想**：打破单一向量瓶颈，用**一组向量** $\{v_u^1, ..., v_u^K\}$ 来表示用户，每个向量代表一个独立的兴趣维度。
* **关键技术**：引入胶囊网络（Capsule Network）的**动态路由**机制，自动将用户历史行为聚类到不同的兴趣胶囊中。

**关键公式：**

1.  **动态路由 (Dynamic Routing) - 软聚类过程**：
    * **路由连接强度**：衡量历史行为 $i$ 归属于兴趣 $j$ 的程度。
        $$b_{ij} = \boldsymbol{u}_j^T \boldsymbol{\textrm{S}} \boldsymbol{e}_i$$
        * $\boldsymbol{e}_i$：用户历史行为向量。
        * $\boldsymbol{S}$：共享的双线性映射矩阵（Shared Bilinear Mapping Matrix）。
    * **非线性压缩 (Squash Function)**：MIND 的核心激活函数，将向量模长压缩至 $[0, 1)$ 代表兴趣存在的概率，同时保持方向不变。
        $$\boldsymbol{u}_j = \text{squash}(\boldsymbol{z}_j) = \frac{\left\lVert \boldsymbol{z}_j \right\rVert ^ 2}{1 + \left\lVert \boldsymbol{z}_j \right\rVert ^ 2} \frac{\boldsymbol{z}_j}{\left\lVert \boldsymbol{z}_j \right\rVert}$$
        * $\boldsymbol{z}_j$：聚类后的加权和向量。

2.  **标签感知注意力 (Label-aware Attention)**：
    * **反向监督**：在训练/预测时，不能直接用所有兴趣向量，而是根据目标物品（Label）动态计算权重，选择最相关的那个兴趣向量。
        $$v_u = V_u \cdot \text{softmax}(\text{pow}(V_u^T e_i, p))$$
        * $V_u$：用户的多个兴趣向量矩阵。
        * $e_i$：目标物品的 Embedding。
        * $p$：调节注意力的超参数（$p$ 越大，注意力越集中，趋向于 Hard Selection）。

##### 1.2 SDM (Sequential Deep Matching)

* **核心思想**：解决**长短期兴趣融合**的问题。MIND 解决了“广度”，SDM 解决“时间”。
* **架构设计**：
    * **短期兴趣**：利用 **LSTM + 多头注意力** 处理最近的 Session 序列，捕捉即时需求。
    * **长期兴趣**：从 **特征维度**（ID、品牌、店铺、类别）聚合历史行为，捕捉稳定偏好。

**关键公式：**

1.  **门控融合 (Gated Fusion)**：
    * 类似于 LSTM 的门控机制，动态决定保留多少长期信息和多少短期信息。
        $$\boldsymbol{G}_{t}^{u} = \operatorname{sigmoid}\left(\boldsymbol{W}^{1} \boldsymbol{e}_{u}+\boldsymbol{W}^{2} \boldsymbol{s}_{t}^{u}+\boldsymbol{W}^{3} \boldsymbol{p}^{u}+\boldsymbol{b}\right)$$
    * **最终输出**：
        $$\boldsymbol{o}_{t}^{u} = \left(1-\boldsymbol{G}_{t}^{u}\right) \odot \boldsymbol{p}^{u}+\boldsymbol{G}_{t}^{u} \odot \boldsymbol{s}_{t}^{u}$$
        * $\boldsymbol{G}_{t}^{u}$：门控系数向量。
        * $\boldsymbol{p}^{u}$：长期兴趣向量（Long-term Preference）。
        * $\boldsymbol{s}_{t}^{u}$：短期兴趣向量（Short-term Preference）。

#### 2. 生成式召回 (Generative Retrieval)

* **核心范式**：不再试图“总结”用户，而是直接将推荐看作**序列预测 (Next Item Prediction)** 问题，类似于 NLP 语言模型预测下一个词。

##### 2.1 SASRec (Self-Attentive Sequential Recommendation)

* **核心思想**：将 NLP 领域的 **Transformer** 直接应用于推荐序列，是该领域的奠基之作。
* **优势**：相比 RNN，既能捕捉长距离依赖（全序列视野），又能实现高效的并行计算。

**关键公式：**

1.  **自注意力机制 (Self-Attention)**：
    * 计算序列中物品之间的相关性。
        $$\text{Attention}(\bf{Q},\bf{K},\bf{V})=\text{softmax}\left(\frac{\bf{Q}\bf{K}^T}{\sqrt{d}}\right)\bf{V}$$
    * **因果掩码 (Causal Mask)**：必须应用 Mask 机制，确保预测 $t$ 时刻时只能看到 $1 \sim t-1$ 时刻的信息，严禁“偷看”未来。

2.  **预测层**：
    * 基于 Transformer 输出的序列表示 $\bf{F}_t$，在全库中检索最相似的物品。
        $$r_{i,t}=\bf{F}_t\bf{M}_i^T$$

##### 2.2 HSTU (Hierarchical Sequential Transduction Unit)

* **核心思想**：**深化输入 (Input Deepening)**。SASRec 仅使用物品 ID 序列，HSTU 将用户属性、行为类型（点击/加购）、时间戳等所有异构特征统一编码为 **“事件流” (Event Stream)**。
* **改进点**：设计了专门的 HSTU 单元替代 Transformer，并改进了聚合方式以保留偏好强度。

**关键公式：**

1.  **点向聚合 (Pointwise Aggregation)**：
    * 放弃 Softmax 归一化（Softmax 会导致概率和为 1，稀释了用户的强偏好信号）。
    * 改用 SiLU 激活函数，保留偏好的**绝对强度**。
        $$A(X)V(X) = \phi_2\left(Q(X)K(X)^T + \text{rab}^{p,t}\right)V(X)$$
        * $\phi_2$：SiLU 激活函数。
        * $\text{rab}^{p,t}$：相对位置编码偏差。

##### 2.3 TIGER (Transformer Index for GEnerative Recommenders)

* **核心思想**：**重塑输出 (Output Reshaping)**。反思“预测原子 ID”的局限性（无语义、冷启动难），提出预测**语义 ID (Semantic ID)**。

**关键流程与公式：**

1.  **语义 ID 生成 (RQ-VAE 量化)**：
    * 使用残差量化变分自编码器，将物品的内容特征（标题、图片）映射为一串离散的码字元组 $(c_0, c_1, ...)$。
    * **量化过程**：在每一层 $d$，寻找与当前残差最接近的码字。
        $$c_d = \arg\min_{k} \|\mathbf{r}_d - \mathbf{e}_k\|^2$$
    * **残差更新**：
        $$\mathbf{r}_{d+1} := \mathbf{r}_d - \mathbf{e}_{c_d}$$
    * **结果**：物品 ID 从无意义的数字（如 `1001`）变为有语义的序列（如 `(12, 5, 8)`，其中 `12` 可能代表“鞋子”，`5` 代表“耐克”）。

2.  **生成式检索**：
    * 模型预测的目标变成了这串语义码字。即便是新物品，只要内容相似，生成的码字也相似，模型即可推荐。

## 二、精准偏好预测

### 1. 记忆与泛化 (Wide & Deep)

* **核心思想**：通过 **联合训练（Joint Training）** 机制，将广义线性模型的记忆能力与深度神经网络的泛化能力无缝融合。
* **两大核心能力**：
    * **记忆 (Memorization)**：记住历史数据中高频共现特征组合的能力（如“买了A通常会买B”），精准捕捉显性强关联。
    * **泛化 (Generalization)**：发掘特征间深层、非线性关联的能力，探索未曾出现的新特征组合，提升推荐的多样性与新颖性。

#### 1.1 Wide 部分

* **核心网络**：本质为广义线性模型（如逻辑回归），结构简单，可解释性强。
* **特征输入**：包含原始特征及大量**人工设计的交叉特征（Cross-product Features）**，旨在快速“记忆”强关联规则。

**关键公式：**

1.  **线性预测**：
    $$y=\mathbf{w}^T \mathbf{x}+b$$
    * $\mathbf{w}$：模型权重。
    * $\mathbf{x}$：特征向量。
    * $b$：偏置项。

#### 1.2 Deep 部分

* **核心网络**：标准前馈神经网络（DNN），自动学习特征间的高阶、非线性关系，摆脱人工特征工程限制。
* **特征映射**：利用 **嵌入层（Embedding Layer）** 将高维稀疏类别特征（如ID）映射为低维稠密向量，捕捉潜在语义信息。

**关键公式：**

1.  **前向传播**：
    $$a^{(l+1)}=f(W^{(l)}a^{(l)}+b^{(l)})$$
    * $a^{(l)}$：第 $l$ 层的激活值。
    * $W^{(l)}$、$b^{(l)}$：第 $l$ 层的权重和偏置。
    * $f$：激活函数（如 ReLU）。

#### 1.3 联合训练 (Joint Training)

* **核心机制**：融合 Wide 和 Deep 两部分的输出进行最终预测，反向传播时同时更新两部分参数。

**关键公式：**

1.  **最终预测概率**：
    $$P(Y=1|\mathbf{x})=\sigma(\mathbf{w}_{wide}^T[\mathbf{x},\phi(\mathbf{x})]+\mathbf{w}_{deep}^T a^{(lf)}+b)$$
    * $\sigma$：Sigmoid 激活函数。
    * $[\mathbf{x}, \phi(\mathbf{x})]$：Wide 部分输入向量（原始特征拼接交叉特征）。
    * $a^{(lf)}$：Deep 部分最后一层输出向量。

* **工程Trick：优化器解耦**：
    * **Wide 部分**：特征极度稀疏，常采用带 **L1 正则化的 FTRL** 优化器（利用L1产生稀疏权重，实现自动特征选择）。
    * **Deep 部分**：参数稠密，常采用 **AdaGrad** 或 **Adam** 优化器。

### 2. 特征交叉

* **核心目标**：实现特征交叉的自动化，替代 Wide & Deep 中依赖人工先验知识的 Wide 部分。

#### 2.1 二阶特征交叉

致力于自动、高效地捕捉所有成对（二阶）特征的交互，并与 DNN 进行融合。

* **FM (隐向量内积与参数共享)**
    * **核心思想**：为每个特征学习一个低维隐向量（Embedding），用向量内积代替独立的交叉权重，有效解决参数量爆炸（$O(n^2) \rightarrow O(nk)$）和数据稀疏问题。
    * **关键公式 (预测公式)**：
        $$y = w_0 + \sum_{i=1}^n w_i x_i + \sum_{i=1}^{n-1} \sum_{j=i+1}^n \langle \mathbf{v}_i, \mathbf{v}_j \rangle x_i x_j$$
        * $\mathbf{v}_i, \mathbf{v}_j$：特征 $i$ 和 $j$ 的隐向量。

* **AFM (注意力加权)**
    * **核心思想**：不同特征交叉组合的重要性不同，在 FM 基础上引入注意力机制，为二阶交叉特征分配自适应权重。
    * **关键公式 (注意力池化)**：
        $$f_{Att} = \sum_{(i,j) \in \mathcal{R}_x} a_{ij} (\mathbf{v}_i \odot \mathbf{v}_j) x_i x_j$$
        * $a_{ij}$：通过注意力网络学习并归一化后的重要性得分。

* **NFM (交叉特征深度学习)**
    * **核心思想**：将 FM 提取的二阶交叉信息（元素积向量）作为“原料”输入 DNN，由 DNN 自动学习高阶非线性关系。
    * **关键公式 (特征交叉池化层 - 线性复杂度优化)**：
        $$f_{BI}(V_x) = \frac{1}{2} \left[\left(\sum_{i=1}^n x_i \mathbf{v}_i\right)^2 - \sum_{i=1}^n (x_i \mathbf{v}_i)^2\right]$$

* **PNN (多样化乘积操作)**
    * **核心思想**：引入内积（IPNN）和外积（OPNN）网络层来显式提取特征交互，替代传统 DNN 中简单的加法操作。
    * **关键Trick (OPNN 外积近似)**：为了降低 $O(N^2M^2)$ 的复杂度，采用“叠加”近似，先将特征相加再算外积：
        $$\sum_{i=1}^N \sum_{j=1}^N \mathbf{f}_i \mathbf{f}_j^T = (\sum_{i=1}^N \mathbf{f}_i)(\sum_{j=1}^N \mathbf{f}_j)^T$$

* **FiBiNET (特征重要性与双线性交互)**
    * **核心思想**：先用 SENET 机制动态学习特征重要性（Squeeze-Excitation-Reweight），再用引入可学习权重矩阵的“双线性交互层”捕捉精细交互。
    * **关键公式 (双线性交互)**：
        $$\mathbf{p}_{ij} = \mathbf{v}_i \cdot \mathbf{W} \circ \mathbf{v}_j$$
        * $\mathbf{W}$：可学习的变换矩阵，$\circ$ 表示哈达玛积。

* **DeepFM (低高阶统一建模)**
    * **核心思想**：直接用 FM 替换 Wide & Deep 的 Wide 部分。**FM 组件与 Deep 组件共享特征 Embedding**，实现真正的端到端联合训练。
    * **关键公式 (融合输出)**：
        $$\hat{y} = \sigma(y_{FM} + y_{Deep})$$

#### 2.2 高阶特征交叉

突破 DNN “隐式”学习高阶交互的黑盒限制，探索显式、可控的高阶特征交叉网络。

* **DCN (残差连接高阶交叉)**
    * **核心思想**：提出 Cross Network，在每一层通过残差连接保留与原始输入 $\mathbf{x}_0$ 的交互，在**元素级别 (bit-wise)** 显式构造任意高阶特征。
    * **关键公式 (第 $l+1$ 层交叉)**：
        $$\mathbf{x}_{l+1} = \mathbf{x}_0 \mathbf{x}_l^T \mathbf{w}_l + \mathbf{b}_l + \mathbf{x}_l$$
        * 交叉项 $\mathbf{x}_0 \mathbf{x}_l^T \mathbf{w}_l$ 使得网络深度决定了特征交叉的最高阶数。

* **xDeepFM (向量级别特征交互)**
    * **核心思想**：提出 CIN (Compressed Interaction Network)，使特征在**向量级别 (vector-wise)** 进行显式交互，更符合特征工程直觉。
    * **关键公式 (CIN 核心压缩交互)**：
        $$\mathbf{X}_{h,*}^k = \sum_{i=1}^{H_{k-1}} \sum_{j=1}^{m} \mathbf{W}_{i,j}^{k,h} (\mathbf{X}_{i,*}^{k-1} \circ \mathbf{X}_{j,*}^0)$$
        * 将上一层输出 $\mathbf{X}^{k-1}$ 与原始输入 $\mathbf{X}^0$ 进行哈达玛积交互，并通过参数矩阵加权“压缩”成新特征图。

* **AutoInt (自注意力自适应交互)**
    * **核心思想**：摒弃固定交互公式，引入多头自注意力机制（Multi-head Self-Attention），让模型自适应决定交互的特征及强度，兼具高性能与强可解释性。
    * **关键公式 (注意力计算与特征融合)**：
        1. **相关性得分**: $\alpha_{m,k}^{(h)} = \frac{\exp(\psi^{(h)}(\mathbf{e}_m, \mathbf{e}_k))}{\sum_{l=1}^{M}\exp(\psi^{(h)}(\mathbf{e}_m, \mathbf{e}_l))}$
        2. **新特征生成**: $\mathbf{\tilde{e}}_m^{(h)} = \sum_{k=1}^{M} \alpha_{m,k}^{(h)} (\mathbf{W}_{\text{Value}}^{(h)} \mathbf{e}_k)$

### 3. 序列建模

* **核心痛点**：传统的特征交叉模型将用户历史行为视为无序的静态“物品袋（bag of items）”，无法捕捉用户兴趣的时序性与动态演化特点。
* **演进方向**：从静态特征组合，转向动态序列挖掘，根据当前候选目标动态调整用户兴趣表示。

#### 3.1 DIN (局部激活与注意力机制)

* **核心思想**：用户兴趣是多样的，其表示不应是一个固定长度的向量，而应根据当前的“候选广告（Target Ad）”动态变化，即“局部激活 (Local Activation)”。
* **技术实现**：引入局部激活单元（本质为注意力机制），计算历史行为与候选广告的相关性，进行加权求和。
* **关键公式 (局部激活)**：
    $$\boldsymbol{v}_{U}(A)=\sum_{j=1}^{H}a(\boldsymbol{e}_{j},\boldsymbol{v}_{A})\boldsymbol{e}_{j}=\sum_{j=1}^{H}w_{j}\boldsymbol{e}_{j}$$
    * $\boldsymbol{e}_{j}$：历史行为 Embedding。
    * $\boldsymbol{v}_{A}$：候选广告 Embedding。
* **工程Trick**：注意力权重 $w_j$ **不经过 Softmax 归一化**（即总和不为 1）。目的在于保留用户兴趣的绝对强度，使向量模长既能表达兴趣方向，又能反映兴趣强弱。

#### 3.2 DIEN (兴趣的演化建模)

* **核心思想**：克服 DIN 忽略序列时序依赖的缺点，认为模型不仅需关注“相关性”，更需提取潜在“兴趣状态”并对其“演化过程”进行建模。
* **两阶段结构**：
    1. **兴趣提取层 (Interest Extractor Layer)**：利用 GRU 处理历史序列，并引入**辅助损失 (Auxiliary Loss)**。用 $t$ 时刻的隐状态去预测 $t+1$ 时刻的真实点击，强制 GRU 隐状态精准表达潜在兴趣。
       **关键公式 (辅助损失)**：
       $$L_{aux}=-\frac{1}{N}\left(\sum_{i=1}^{N}\sum_{t=1}^{T}\log\sigma(\boldsymbol{h}^i_t,\boldsymbol{e}^i_{b[t+1]})+\log(1-\sigma(\boldsymbol{h}^i_t,\boldsymbol{\hat{e}}^i_{b[t+1]}))\right)$$
    2. **兴趣演化层 (Interest Evolving Layer)**：为了克服“兴趣漂移（Interest Drifting）”，设计了带注意力更新门的 GRU（**AUGRU**）。利用候选广告计算注意力得分，缩放 GRU 的更新门，使模型聚焦于与当前推荐任务相关的兴趣演化路径。
       **关键公式 (更新门缩放)**：
       $$\boldsymbol{\tilde{u}}'_t = a_t \cdot \boldsymbol{u}'_t$$

#### 3.3 DSIN (会话序列分层建模)

* **核心思想**：用户的真实行为具有“会话内同质、会话间异质”的断层特性。DSIN 放弃直接处理长序列，改为将会话（Session）作为基本单元进行分层建模。
* **网络架构 (分层处理)**：
    1. **会话划分层**：按时间间隔（如30分钟）将连续长序列切分为多个会话短序列。
    2. **会话兴趣提取层**：在会话内部，使用 **自注意力机制（Self-Attention）** 进行信息聚合，为每个会话生成一个浓缩兴趣向量 $\mathbf{I}_k$。
    3. **会话兴趣交互层**：在会话之间，使用 **双向 LSTM（Bi-LSTM）** 对会话兴趣向量序列进行建模，捕捉跨会话的长程演进依赖，输出 $\mathbf{H}_k$。
    4. **会话兴趣激活层**：复用 DIN 思想，用候选广告分别对 $\mathbf{I}_k$ 和 $\mathbf{H}_k$ 计算注意力权重并加权求和，最终拼接得到用户的最终兴趣表示。

### 4. 多目标建模

在真实的推荐场景中，往往需要同时优化多个业务目标（如点击率、转化率、停留时长等），多目标建模旨在平衡不同任务间的关联与冲突。

#### 4.1 基础结构演进

旨在解决多任务间的特征共享与冲突（跷跷板现象/负迁移）问题。

* **Shared-Bottom (共享底层)**
    * **核心思想**：“共享地基+独立塔楼”设计。所有任务共用底层的特征转换层，顶层各自拥有独立的任务塔网络。
    * **优势**：显著降低参数量；共享层具天然正则化效应；相关任务间可实现知识迁移。
    * **致命缺陷 (负迁移)**：当任务目标存在冲突（如点击率与客单价）时，共享层梯度方向发生矛盾（$\nabla L_i \cdot \nabla L_j < 0$），导致“零和博弈”。
    * **关键公式**：
        $$\hat{y}_t = f_t(W_t \cdot g(W_s \mathbf{x}))$$

* **MMoE (Multi-gate Mixture-of-Experts)**
    * **核心思想**：将共享底层拆分为多个专家网络（Expert），并为每个任务配备专属门控网络（Gate），实现门控的“任务自适应”选择。
    * **优势**：任务冲突时，不同门控对专家的加权融合产生差异。某任务主导某专家更新时，能起到一定的梯度隔离效果，缓解负迁移。
    * **关键公式 (任务 $t$ 的融合输出)**：
        $$\mathbf{h}_t = \sum_{k=1}^K g_{t,k} \cdot \mathbf{e}_k$$

* **PLE (Progressive Layered Extraction)**
    * **核心思想**：针对 MMoE 中“所有专家对所有门控可见”导致的干扰和重决策负担，提出 CGC (Customized Gate Control) 结构进行硬隔离，并纵向堆叠实现渐进式特征提取。
    * **网络结构 (CGC 强制分离)**：
        * 将专家显式划分为**共享专家 (C-Experts)**与**任务专属专家 (T-Experts)**。
        * 任务 $t$ 的门控**仅能**接收共享专家和自身专属专家的输出，物理切断其他任务的梯度干扰路径。

#### 4.2 任务依赖建模

针对真实场景中严格的时序行为链路（如：曝光 $\rightarrow$ 点击 $\rightarrow$ 转化）进行概率建模，解决**样本选择偏差**和**数据稀疏性**两大核心痛点。

* **ESMM (Entire Space Multi-task Model)**
    * **核心思想**：利用概率转化图，引入 CTCVR（点击且转化）任务作为辅助，使得 CVR 塔的参数更新能涵盖**全量曝光样本空间**。
    * **关键公式 (概率约束与 Loss)**：
        1. 概率链式法则：$pCTCVR = pCTR \times pCVR$
        2. 全空间 Loss：$\mathcal{L}_{CTCVR} = - \frac{1}{N} \sum_{i=1}^N \left[ y_i^{click} \cdot y_i^{conv} \log(pCTCVR_i) + (1 - y_i^{click} \cdot y_i^{conv}) \log(1 - pCTCVR_i) \right]$

* **ESM2**
    * **核心思想**：将 ESMM 思想扩展至更复杂的长链路（如：曝光 $\rightarrow$ 点击 $\rightarrow$ 决定行为 $\rightarrow$ 购买）。分解链路为多个条件概率节点，确保每个子任务都在曝光空间中联合优化。
    * **关键公式 (多路径合并预测)**：
        $$pCTCVR=y_1(y_2 \cdot y_3 + (1-y_2) \cdot y_4)$$
        * $y_1$：点击率，$y_2$：决定行为率，$y_3$、$y_4$：不同路径下的购买率。

#### 4.3 多目标损失融合

解决传统手工设定权重导致的量级失衡、收敛异步与梯度冲突问题。

* **Uncertainty Weight (基于不确定性的自适应加权)**
    * **核心思想**：引入可学习参数 $\sigma$ 代表任务不确定性。模型自动判定，任务 Loss 越大则权重被分配得越小，避免模型往不确定性大的方向剧烈更新。
    * **关键公式**：
        $$Loss \approx \frac{1}{2\sigma_1^2} \mathcal{L}_1(\mathbf{W}) + \frac{1}{\sigma_2^2} \mathcal{L}_2(\mathbf{W}) + \log \sigma_1 + \log \sigma_2$$

* **GradNorm (梯度标准化)**
    * **核心思想**：引入独立的 Gradient Loss。动态监控各任务梯度的量级和 Loss 的收敛速度，当某任务梯度过大或收敛过快时，自动调小其 Loss 权重，防止单一任务主导模型。

* **Pareto Optimization (帕累托优化框架)**
    * **核心思想**：专治任务梯度严重冲突问题。利用 KKT 条件动态寻找权重组合，使优化方向逼近帕累托前沿（即在不损害其他任务的前提下优化目标）。
    * **关键机制 (权重学习转化为二次规划)**：
        * 固定模型更新权重 $w_i$ 时，目标是最小化加权梯度的二范数：$\min _{w}\left\|\sum_{i=1}^{K} w_{i} \nabla_{\theta} \mathcal{L}_{i}(\theta)\right\|_{2}^{2}$。

### 5. 多场景建模

在现代大规模推荐系统中，用户的行为和兴趣往往呈现出高度的**场景依赖性**。这里的"场景"可以指不同的业务形态（如电商APP的首页推荐、商品详情页的"猜你喜欢"）、不同的流量入口、不同的用户状态等。

* **核心挑战**：
    1.  **共性淹没特性**：强行融合所有场景数据训练，模型容易被数据量大的主导场景所主导，难以捕捉小场景的独特模式。
    2.  **数据稀疏性**：对于新场景、小流量场景，独立建模所需的高质量训练数据往往不足。
    3.  **参数效率低下**：为每个场景独立训练和维护一个完整模型，成本高昂且难以实现场景间的知识迁移。

* **核心目标**：**既要"合"得好（共享有益知识），也要"分"得清（保留独有特性）**。

* **技术范式**：
    1.  **多塔结构建模**：在模型结构层面进行显式划分，为共享知识构建公共塔，为每个场景的特性知识构建独立场景塔。
    2.  **动态权重建模**：利用输入样本自身的场景上下文信息，动态调整模型内部组件的计算方式或重要性。

#### 5.1 多塔结构

多塔结构借鉴了多任务学习（如 MMoE）中"共享专家 + 任务专属门控"的架构思想，底层通过多专家网络提取多场景的共享特征，顶层为不同场景构建独立的塔结构。

* **多场景 vs 多任务**：
    * **多任务学习**：处理相同场景/分布下的不同任务（如单样本同时预估CTR、CVR），对于一条样本预估多个不同的目标值。
    * **多场景建模**：处理不同场景/分布下的相同任务（如不同场景预估相同CTR），对于不同的样本预估相同的目标值。

##### HMoE (Hierarchical Mixture of Experts)

* **核心思想**：借鉴 MMoE 的思路，底层使用多专家网络提取多场景的共享特征，顶层为不同场景构建独立的塔结构，并通过门控机制融合专家特征。

**关键公式：**

1.  **专家特征融合**：
    $$M(x) = \sum_{i=1}^{K} G_i(x) E_i(x)$$
    * $E_i(x)$：第 $i$ 个专家网络的输出。
    * $G_i(x)$：门控网络的权重输出。

2.  **场景专属门控融合**（可选优化）：
    $$M_t(x) = \sum_{i=1}^{K} G_i^t(x) E_i(x)$$
    * 不同场景使用不同的门控网络，避免场景相关性差时的负迁移。

3.  **多场景打分融合**：
    $$out_t = \sum_{i=1}^{T} W_i(x) S_i(x)$$
    * $S_i(x)$：场景 $i$ 的塔输出打分。
    * $W_i(x)$：场景 $i$ 的融合权重。

4.  **梯度阻断机制**：
    * 对于场景 $t$ 的样本，应该只影响当前场景的参数，否则会导致模型对场景的感知下降。
    $$out_t(x) = W_t(x) S_t(x) + \sum_{j=1, j \neq t}^{T} W_j(x) \underbrace{S_j(x)}_{\text{stop gradient}}$$
    * 其他场景的打分作为参考信息，但**阻断梯度回传**，避免跨场景干扰。

##### STAR (Star Topology Adaptive Recommender)

* **核心思想**：采用**星型拓扑结构**，实现场景私有参数和场景共享参数同时建模。每个场景最终的参数由共享参数和独占参数通过 **element-wise product** 融合计算得到。

**关键创新：**

1.  **星型拓扑全连接网络 (STAR Topology FCN)**：
    * 对于每一个全连接网络（FCN）都有场景共享和场景独占两部分参数。
    $$W_p^{\star} = W_p \otimes W$$
    $$b_p^{\star} = b_p + b$$
    * $W_p, W$：分别表示第 $p$ 个场景独有和全场景共享的权重参数。
    * $b_p, b$：分别表示场景独有和共享的偏置参数。
    * $\otimes$：element-wise product（逐元素乘积）。

2.  **层输出计算**：
    $$out_p = \phi((W_p^\star)^\top in_p + b_p^\star)$$
    * $\phi$：激活函数。
    * $in_p$：第 $p$ 个场景 FCN 的输入。

3.  **分区归一化 (Partitioned Normalization, PN)**：
    * **动机**：传统 BN 在多场景混合样本上得到的统计量会忽略不同场景独有的分布差异。
    * **解决方案**：让不同场景独享统计量，保证场景内的独立同分布假设。
    * PN 在训练时对不同场景独立计算均值和方差，推理时使用对应场景的统计量。

4.  **辅助网络 (Auxiliary Network)**：
    * 将场景 ID 等场景上下文特征作为额外输入，帮助模型显式感知当前样本所属场景。
    * 辅助网络的输出与主网络输出相加，增强场景区分能力。

**STAR 的优势**：
* **参数高效**：通过参数分解（共享 + 独占），避免了为每个场景维护完整模型的资源消耗。
* **知识迁移**：共享参数使大场景的知识能够迁移到小场景，缓解数据稀疏问题。
* **场景差异化**：独占参数保证了每个场景的个性化表达能力。

#### 5.2 动态权重建模

多塔结构通过"物理隔离"的参数空间来保障场景的独特性，而动态权重建模则采用不同的思路：让模型的核心网络参数在不同场景下共享一个基础，但通过动态生成的、与场景/样本高度相关的"权重"来调制（Modulate）这些共享参数的行为。这相当于为共享网络"注入"了场景和样本的上下文信息。

##### PEPNet (Parameter and Embedding Personalized Network)

* **核心目标**：解决多场景多任务中的**双重跷跷板效应**。
    * **场景跷跷板 (Domain Seesaw)**：混合训练时不同场景数据分布差异导致表征无法对齐。
    * **任务跷跷板 (Task Seesaw)**：多任务间稀疏性与依赖关系失衡导致目标相互抑制。

* **核心组件：Gate NU (门控单元)**
    * 受语音识别领域 LHUC 模型启发，通过两层网络生成动态缩放权重：
    $$\mathbf{x'} = \text{ReLU}(\mathbf{x} \mathbf{W_1} + \mathbf{b_1})$$
    $$\delta = \gamma \cdot \text{Sigmoid}(\mathbf{x'} \mathbf{W_2} + \mathbf{b_2}) \quad \in [0, \gamma]$$
    * $\mathbf{x}$：个性化先验特征（如场景 ID 或用户画像）。
    * $\gamma$：缩放强度（经验值设为 2）。
    * 输出 $\boldsymbol{\delta}$ 与目标参数维度对齐，通过逐元素相乘实现调制。

* **EPNet：场景感知的嵌入个性化**
    * **动机**：共享底层 Embedding 强调场景共性，忽略了不同场景下 Embedding 的差异性。
    * **关键公式**：
        $$\delta_{domain} = \mathcal{U}_{ep}(E(\mathcal{F}_d) \oplus (\oslash(E)))$$
        $$O_{ep} = \delta_{domain} \otimes E$$
    * $E(\mathcal{F}_d)$：场景先验特征的 Embedding。
    * $\oslash(E)$：Stop Gradient 操作，防止个性化模块影响底层共享 Embedding 的学习。
    * 通过元素级乘积得到场景个性化的 Embedding 表征。

* **PPNet：用户感知的参数个性化**
    * **动机**：解决多任务之间的跷跷板问题，实现样本粒度的个性化。
    * **关键公式**：
        $$O_{prior} = E(F_u) \oplus E(F_i) \oplus E(F_a)$$
        $$\delta_{task} = \mathcal{U}_{pp}(O_{prior} \oplus (\odot(O_{ep})))$$
    * $F_u, F_i, F_a$：用户 ID、内容 ID、作者 ID 作为个性化先验特征。
    * **DNN 层参数个性化**：
        $$\mathbf{O}^{(l)}_{pp} = \delta^{(l)}_{task} \otimes \mathbf{H}^{(l)}$$
        $$\mathbf{H}^{(l+1)} = f(\mathbf{O}^{(l)}_{pp} \mathbf{W}^{(l)} + \mathbf{b}^{(l)})$$

##### APG (Adaptive Parameter Generation)

* **核心思想**：为不同的样本动态生成模型参数，根据样本的不同生成相应的参数，从而提升模型的容量和表达能力。

* **三种样本条件表示策略**：
    1.  **Group-wise**：适用于样本可分组的情况，将 Group 相关特征作为输入。
    2.  **Mix-wise**：综合考虑多种因素，实现更细粒度的样本组划分（如 `<user, item>` pair 向量）。
    3.  **Self-wise**：不需要先验知识，直接将 Deep CTR Models 的隐层输出作为参数生成的输入。

* **自适应参数生成**：
    $$\mathbf{W}_i = \text{reshape}(\text{MLP}(\mathbf{z}_i))$$
    $$y_i = \sigma(\mathbf{W}_i \mathbf{x}_i)$$

* **工程优化：低秩参数分解**
    * **动机**：直接生成完整参数矩阵计算和存储开销大。
    * **解决方案**：将参数矩阵分解为三个子矩阵相乘：
        $$\mathbf{U}_i, \mathbf{S}_i, \mathbf{V}_i = \text{reshape}(\text{MLP}(\mathbf{z}_i))$$
        $$y_i = \sigma(\mathbf{U}_i (\mathbf{S}_i (\mathbf{V}_i \mathbf{x}_i)))$$
    * 通过设置较小的秩值控制开销，分解前向计算避免子矩阵乘操作。

* **参数共享与过参数化**：
    * 将参数矩阵分为**私有参数**（刻画样本特性）和**共享参数**（刻画样本共性）。
    * 共享参数用两个大矩阵替代，增加参数数量提升模型容量，同时具有隐含正则效果：
        $$\mathbf{S}_{i}=\text{reshape}(\text{MLP}(\mathbf{z}_{i}))$$
        $$\mathbf{U} = \mathbf{U}^l \mathbf{U}^r, \mathbf{V} = \mathbf{V}^l \mathbf{V}^r$$
        $$y_{i}=\sigma(\mathbf{U}(\mathbf{S}_{i}\mathbf{V}\mathbf{x}_{i}))$$

## 三、重排模型

在推荐系统流程中，召回阶段从海量物品中筛选出数百到数千候选，精排阶段利用复杂模型对候选集精准打分。然而，仅按预估分数排序的列表往往并非最优推荐结果，面临**多样性缺失**和**上下文忽略**两大挑战。

* **核心挑战**：
    1.  **多样性缺失与审美疲劳**：列表顶部高度同质化，连续推荐相似内容易引发用户厌倦。
    2.  **上下文与整体性忽略**：精排模型较少考虑列表物品间的关联性、互补性或排斥性。

* **重排目标**：对精排输出的高质量候选列表进行全局优化，生成更能满足用户体验需求和业务目标的最终推荐列表。

### 1. 基于贪心的重排

贪心算法以思路直观、计算高效、易于实现的特点，成为重排阶段解决多样性问题的首选策略。

#### 1.1 MMR (Maximal Marginal Relevance)

* **核心目标**：在保留高相关性物品的前提下，通过主动引入多样性打破同质化，实现"相关性与多样性的帕累托最优"。

* **边际收益函数**：
    $$MR(i) = \lambda \cdot \underbrace{\text{Rel}(i)}_{\text{相关性}} - (1-\lambda) \cdot \underbrace{\max_{j \in S} \text{Sim}(i,j)}_{\text{多样性惩罚项}}$$
    * $S$：已选物品集合。
    * $\text{Rel}(i)$：物品 $i$ 的相关性分数（精排模型输出）。
    * $\text{Sim}(i,j)$：物品 $i$ 与 $j$ 的相似度。
    * $\lambda$：权衡参数（$\lambda \to 1$ 退化为精排序，$\lambda \to 0$ 强制多样性优先）。

* **滑动窗口优化**：
    $$MR_{\text{win}}(i) = \lambda \cdot \text{Rel}(i) - (1-\lambda) \cdot \underbrace{\max_{j \in W} \text{Sim}(i,j)}_{\text{窗口多样性惩罚}}$$
    * $W \subseteq S$ 是最近选择的 $w$ 个物品，降低计算复杂度。

#### 1.2 DPP (Determinantal Point Process)

* **核心优势**：MMR 通过两两相似度贪心选择，无法捕捉多个物品间的复杂排斥关系。DPP 利用行列式的几何意义实现更精确的多样性控制。

* **行列式度量多样性**：
    * 矩阵行列式的几何意义是列向量张成的超面体的"有向体积"。
    * 列向量线性相关 $\Rightarrow$ 行列式为 0 $\Rightarrow$ 多样性低。
    * 列向量线性不相关 $\Rightarrow$ 行列式大 $\Rightarrow$ 多样性高。

* **核矩阵构造（融合相关性与多样性）**：
    $$\mathbf{L}_{ij} = \langle \mathbf{B}_i, \mathbf{B}_j \rangle = r_i r_j \langle \mathbf{f}_i, \mathbf{f}_j \rangle$$
    $$\mathbf{L} = \text{Diag}(\mathbf{r}) \cdot \mathbf{S} \cdot \text{Diag}(\mathbf{r})$$
    * $r_i$：物品 $i$ 的相关性得分。
    * $\mathbf{S}$：物品相似度矩阵。

* **目标函数推导**：
    $$\log |L_{R_u}| = \sum_{i \in R_u} \log r_{u,i}^2 + \log |S|$$
    * 第一项只与"相关性"有关，越相关 $r_{u,i}^2$ 越大。
    * 第二项只与"多样性"有关，$S$ 越接近正交，行列式越大。
    * 引入超参 $\theta$ 平衡权重：
        $$\log |L_{R_u}| = \theta \sum_{i \in R_u} \log r_{u,i}^2 + (1-\theta) \log |S|$$

* **贪心求解（Cholesky 分解加速）**：
    * 每次选择物品 $j$ 使边际收益最大：
        $$j = \arg\max_{i \in Z \setminus Y_g} \log\det(\mathbf{L}_{Y_g \cup \{i\}}) - \log\det(\mathbf{L}_{Y_g})$$
    * 利用 Cholesky 分解 $L_{Y_g} = VV^T$，增量计算避免重复分解。

### 2. 基于个性化的重排

贪心方法依赖预设的多样性公式，难以捕捉高阶、非线性的交互模式，也难以深度融入用户个性化信息。

#### 2.1 PRM (Personalized Re-Ranking Model)

* **核心思想**：利用 Transformer 的序列建模能力自动学习列表中物品间复杂的相互影响，并将细粒度的用户个性化信息深度融入整个重排序过程。

* **模型架构**：
    1.  **输入层**：
        $$E = [\text{物品特征}(x_j); \text{个性化向量}(pv_j)] + \text{位置嵌入}(pe_j)$$
        * 物品自身特征 $x_j$：物品 ID 嵌入、类别、标签等。
        * 个性化向量 $pv_j$：编码用户 $u$ 与物品 $i_j$ 之间的独特偏好。

    2.  **编码层**：
        * 利用 Transformer 的自注意力机制，让列表中所有物品充分"沟通"和"互动"。
        * 捕捉长距离的物品间依赖关系（替代品、互补品、多样性）。

    3.  **输出层**：
        * 对每个物品的高级表示应用线性变换得到标量分数。
        * Softmax 归一化为概率分布，隐含相对关系建模。

* **个性化向量 (PV) 生成**：
    * 利用预训练的 CTR 模型生成 PV。
    * 提取输出层之前的隐藏层激活值作为 $pv_i$。
    * 该向量蕴含了"用户 $u$ 对物品 $i$ 偏好程度"的丰富信息。

#### 2.2 PRS (Permutation Re-Ranking System)

* **核心洞察：排列变异影响 (Permutation-Variant Influence)**：
    * 用户面对不同排列的同一组物品，行为可能截然不同。
    * 例如：将高价商品 B 放在前面，会让用户觉得商品 A 相对便宜，激发购买欲望。

* **两阶段设计**：
    1.  **PMatch 阶段**：通过智能搜索算法快速筛选出少数几个"有希望"的候选排列。
    2.  **PRank 阶段**：使用神经网络模型精确评估候选排列的质量，选出最优解。

* **PMatch 阶段：FPSA 算法**：
    * **双模型预测体系**：
        * **CTR 模型**：预测用户点击某物品的概率 $P_{CTR}(i|u)$。
        * **Next 模型**：预测用户浏览完当前物品后继续浏览的概率 $P_{Next}(i|u)$。
    * **关键奖励指标**：
        * **rPV (Page View Reward)**：衡量排列能带来的总浏览深度。
        * **rIPV (Item Page View Reward)**：衡量排列中物品被点击的总概率。
    * **Beam Search 搜索**：
        $$r_{sum} = \alpha \cdot r_{pv} + \beta \cdot r_{ipv}$$
        * 通过树状搜索逐步构建候选排列，每步基于奖励函数剪枝。

* **PRank 阶段**：
    * 使用专门的神经网络模型精确评估候选排列的质量。
    * 综合考虑用户偏好、物品特征和排列顺序的交互影响。

### 3. 重排技术演进总结

| 方法 | 核心思想 | 优势 | 局限 |
|------|----------|------|------|
| MMR | 相关性与多样性线性组合 | 计算高效、易于实现 | 仅考虑两两相似度 |
| DPP | 行列式几何意义度量多样性 | 理论严谨、捕捉高阶关系 | 计算复杂度较高 |
| PRM | Transformer 端到端学习 | 深度个性化、捕捉长程依赖 | 忽略排列顺序影响 |
| PRS | 排列感知的两阶段优化 | 捕捉排列变异影响 | 计算开销最大 |